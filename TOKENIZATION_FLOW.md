# Diagrama de Flujo: Tokenización para Minishell (42)

## 📋 Visión General

La tokenización es el primer paso del parsing. Convierte la entrada del usuario en tokens identificables.

---

## 🔄 Flujo Principal de Tokenización

```
┌─────────────────────────────────────┐
│    INICIO: Leer línea de entrada    │
│               (readline)            │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ¿Línea vacía o solo espacios?     │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │ Sí            │ No
       ▼               ▼
   [RETORNAR]    ┌─────────────────┐
                 │ Inicializar:    │
                 │ - pos = 0       │
                 │ - tokens = []   │
                 └────────┬────────┘
                          │
                          ▼
         ┌────────────────────────────────┐
         │  MIENTRAS pos < longitud_línea │
         └────────────┬───────────────────┘
                      │
                      ▼
         ┌────────────────────────────────┐
         │   ¿Es espacio/tab/newline?     │
         └────────────┬───────────────────┘
                      │
              ┌───────┴───────┐
              │ Sí            │ No
              ▼               ▼
         [pos++]      ┌──────────────────┐
         [continue]   │ Llamar a:        │
                      │ get_next_token() │
                      └────────┬─────────┘
                               │
                               ▼
              ┌────────────────────────────────┐
              │   Agregar token a lista        │
              │   Avanzar pos según token      │
              └────────┬───────────────────────┘
                       │
                       ▼
              ┌────────────────────────────────┐
              │   ¿Error de sintaxis?          │
              └────────┬───────────────────────┘
                       │
               ┌───────┴────────┐
               │ Sí             │ No
               ▼                ▼
         [Error y exit]   [Continuar loop]
                                │
                                ▼
                    ┌──────────────────────┐
                    │  FIN: Retornar lista │
                    │  de tokens           │
                    └──────────────────────┘
```

---

## 🎯 Función get_next_token()

```
                    ┌──────────────────────┐
                    │   get_next_token()   │
                    │   char *str, int pos │
                    └──────────┬───────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  ¿Qué carácter es?   │
                    └──────────┬───────────┘
                               │
       ┌───────────────────────┼───────────────────────┐
       │           │           │           │           │
       ▼           ▼           ▼           ▼           ▼
   ┌──────┐   ┌──────┐   ┌──────┐   ┌──────┐   ┌──────┐
   │  |   │   │  <   │   │  >   │   │  '   │   │  "   │
   └───┬──┘   └───┬──┘   └───┬──┘   └───┬──┘   └───┬──┘
       │          │          │          │          │
       ▼          ▼          ▼          ▼          ▼
   [PIPE]    [Revisar]  [Revisar]  [SINGLE]   [DOUBLE]
              si es >>   si es >>   [QUOTE]    [QUOTE]
              [APPEND]   [APPEND]
              sino       sino
              [REDIR_IN] [REDIR_OUT]
       │          │          │          │          │
       └──────────┴──────────┴──────────┴──────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │   ¿Es otro char?     │
                    │   → WORD             │
                    └──────────┬───────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  Retornar TOKEN con: │
                    │  - tipo              │
                    │  - valor             │
                    │  - longitud          │
                    └──────────────────────┘
```

---

## 🔤 Manejo de WORD (Palabras/Comandos)

```
┌─────────────────────────────────────┐
│   Detectar inicio de WORD           │
│   (no es: |<>'" o espacio)          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   start = pos                       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   MIENTRAS carácter actual NO sea:  │
│   - espacio/tab/newline             │
│   - |, <, >                         │
│   - comilla simple o doble          │
│   - final de string                 │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ¿Es '\' (backslash)?              │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │ Sí            │ No
       ▼               ▼
   [Escapar]      [pos++]
   [next char]    [Continuar]
   [pos += 2]
       │               │
       └───────┬───────┘
               │
               ▼
┌─────────────────────────────────────┐
│   Crear TOKEN tipo WORD             │
│   valor = substring(start, pos)     │
└──────────────┬──────────────────────┘
               │
               ▼
         [RETORNAR TOKEN]
```

---

## 💬 Manejo de QUOTES (Comillas)

### Comilla Simple (')

```
┌─────────────────────────────────────┐
│   Detectar: '                       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   pos++  (saltar comilla inicial)   │
│   start = pos                       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   BUSCAR siguiente '                │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
  [Encontrada]    [NO encontrada]
       │               │
       │               ▼
       │         [ERROR: unclosed]
       │         [quote]
       │
       ▼
┌─────────────────────────────────────┐
│   Extraer contenido entre comillas  │
│   NO interpretar nada (literal)     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   pos++ (saltar comilla final)      │
│   Crear TOKEN tipo WORD             │
│   valor = contenido literal         │
└──────────────┬──────────────────────┘
               │
               ▼
         [RETORNAR TOKEN]
```

### Comilla Doble (")

```
┌─────────────────────────────────────┐
│   Detectar: "                       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   pos++  (saltar comilla inicial)   │
│   start = pos                       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   MIENTRAS != " y != '\0'           │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ¿Es $ (variable)?                 │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │ Sí            │ No
       ▼               ▼
   [Marcar para]   [Añadir char]
   [expansión]     [al resultado]
   [más tarde]     [pos++]
       │               │
       └───────┬───────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ¿Es \ (escape)?                   │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │ Sí            │ No
       ▼               ▼
   [Escapar: $, "]  [Continuar]
   [", \, newline]
       │               │
       └───────┬───────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ¿Encontrada " final?              │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │ Sí            │ No
       ▼               ▼
   [pos++]        [ERROR: unclosed]
   [Crear TOKEN]  [quote]
       │
       ▼
   [RETORNAR TOKEN]
```

---

## ⚙️ Manejo de Operadores

### Pipes ( | )

```
┌─────────────────────┐
│   Detectar: |       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│   Crear TOKEN       │
│   tipo: PIPE        │
│   valor: "|"        │
│   pos++             │
└──────┬──────────────┘
       │
       ▼
  [RETORNAR TOKEN]
```

### Redirecciones ( <, >, <<, >> )

```
┌─────────────────────────────────────┐
│   Detectar: < o >                   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   char actual = str[pos]            │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │ <             │ >
       ▼               ▼
┌────────────┐   ┌────────────┐
│ ¿str[pos+1]│   │ ¿str[pos+1]│
│   == '<'?  │   │   == '>'?  │
└─────┬──────┘   └─────┬──────┘
      │                │
  ┌───┴───┐        ┌───┴───┐
  │Sí     │No      │Sí     │No
  ▼       ▼        ▼       ▼
[HEREDOC][INPUT] [APPEND][OUTPUT]
  <<      <        >>      >
[pos+=2] [pos++] [pos+=2] [pos++]
  │       │        │       │
  └───────┴────────┴───────┘
               │
               ▼
    ┌──────────────────────┐
    │  Crear TOKEN con:    │
    │  - tipo apropiado    │
    │  - valor             │
    └──────────┬───────────┘
               │
               ▼
         [RETORNAR TOKEN]
```

---

## 📊 Tipos de Token (Enum)

```c
typedef enum e_token_type
{
    TOKEN_WORD,        // comando, argumento, o palabra
    TOKEN_PIPE,        // |
    TOKEN_REDIR_IN,    // <
    TOKEN_REDIR_OUT,   // >
    TOKEN_APPEND,      // >>
    TOKEN_HEREDOC,     // <<
    TOKEN_SINGLE_QUOTE,// contenido entre ''
    TOKEN_DOUBLE_QUOTE,// contenido entre ""
    TOKEN_ENV_VAR,     // $VARIABLE
    TOKEN_EXIT_STATUS, // $?
    TOKEN_EOF          // fin de entrada
}   t_token_type;
```

---

## 🏗️ Estructura de Token

```c
typedef struct s_token
{
    t_token_type    type;      // tipo de token
    char            *value;    // contenido del token
    int             length;    // longitud del valor
    struct s_token  *next;     // siguiente token (lista enlazada)
}   t_token;
```

---

## ✅ Casos Especiales a Manejar

### 1. Variables de Entorno ($)
```
echo $USER          → [WORD:"echo"] [ENV_VAR:"USER"]
echo "$USER"        → [WORD:"echo"] [DOUBLE_QUOTE:"$USER"]
echo '$USER'        → [WORD:"echo"] [SINGLE_QUOTE:"$USER"]
```

### 2. Exit Status ($?)
```
echo $?             → [WORD:"echo"] [EXIT_STATUS]
```

### 3. Comillas Vacías
```
echo ""             → [WORD:"echo"] [WORD:""]
echo ''             → [WORD:"echo"] [WORD:""]
```

### 4. Múltiples Espacios
```
echo    hello       → [WORD:"echo"] [WORD:"hello"]
```

### 5. Redirecciones sin Espacios
```
cat<file            → [WORD:"cat"] [REDIR_IN] [WORD:"file"]
echo>file           → [WORD:"echo"] [REDIR_OUT] [WORD:"file"]
```

---

## 🚨 Validaciones Importantes

```
┌─────────────────────────────────────┐
│   DURANTE LA TOKENIZACIÓN           │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ✓ Comillas sin cerrar             │
│   ✓ Caracteres inválidos            │
│   ✓ Tokens vacíos                   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   DESPUÉS DE LA TOKENIZACIÓN        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   ✓ Pipe al inicio o final          │
│   ✓ Pipes consecutivos (||)         │
│   ✓ Redirección sin archivo         │
│   ✓ Comandos vacíos entre pipes     │
└─────────────────────────────────────┘
```

---

## 📝 Ejemplo Completo

### Input:
```bash
echo "Hello World" | grep Hello > output.txt
```

### Proceso de Tokenización:

```
Paso 1: "echo"
  └→ [TOKEN_WORD: "echo"]

Paso 2: skip espacio

Paso 3: "Hello World" (entre comillas dobles)
  └→ [TOKEN_WORD: "Hello World"]

Paso 4: skip espacio

Paso 5: |
  └→ [TOKEN_PIPE: "|"]

Paso 6: skip espacio

Paso 7: "grep"
  └→ [TOKEN_WORD: "grep"]

Paso 8: skip espacio

Paso 9: "Hello"
  └→ [TOKEN_WORD: "Hello"]

Paso 10: skip espacio

Paso 11: >
  └→ [TOKEN_REDIR_OUT: ">"]

Paso 12: skip espacio

Paso 13: "output.txt"
  └→ [TOKEN_WORD: "output.txt"]
```

### Lista Final de Tokens:
```
[WORD:"echo"] → [WORD:"Hello World"] → [PIPE:"|"] → [WORD:"grep"] 
  → [WORD:"Hello"] → [REDIR_OUT:">"] → [WORD:"output.txt"] → NULL
```

---

## 🎓 Orden de Implementación Recomendado

1. **Fase 1**: Estructura básica y tipos
   - Definir `t_token_type`
   - Definir `t_token`
   - Función para crear tokens

2. **Fase 2**: Tokenización simple
   - Detectar espacios
   - Tokenizar palabras simples
   - Operador pipe (|)

3. **Fase 3**: Redirecciones
   - < y >
   - << y >>

4. **Fase 4**: Comillas
   - Comillas simples (literal)
   - Comillas dobles (con expansión)

5. **Fase 5**: Variables
   - $VARIABLE
   - $?

6. **Fase 6**: Validaciones y errores
   - Sintaxis de quotes
   - Sintaxis de operadores

---

## 🔍 Testing

Casos de prueba esenciales:

```bash
# Básico
echo hello

# Pipes
echo hello | cat | wc

# Redirecciones
cat < input.txt > output.txt

# Append y heredoc
echo hello >> file.txt
cat << EOF

# Comillas
echo "hello world"
echo 'hello world'
echo "hello'world"

# Variables
echo $USER $PATH $?

# Combinado
echo "$USER says 'hello'" | cat > file.txt

# Edge cases
echo ""
echo ''
cat<<EOF
echo>file
```

---

## 💡 Tips de Implementación

1. **Usa listas enlazadas** para los tokens (fácil de recorrer)
2. **Maneja memoria cuidadosamente** (free al final)
3. **Valida después de tokenizar** (más fácil que durante)
4. **Testing incremental** (añade casos de prueba gradualmente)
5. **Debug con prints** (imprime cada token mientras desarrollas)

---

¡Este diagrama cubre todo el proceso de tokenización para minishell! 🚀
